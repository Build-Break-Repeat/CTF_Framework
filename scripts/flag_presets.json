{
  "presets": {
    "classic": {
      "description": "One flag per team for each challenge",
      "template": "flag:{{{challenge}_team{team}}}"
    },
    "lab": {
      "description": "Readable flags with challenge and team labels",
      "template": "BBR{{{challenge}-team{team}}}"
    },
    "short": {
      "description": "Short team flag names for quick testing",
      "template": "flag:{{t{team}_{challenge}}}"
    }
  }
}
